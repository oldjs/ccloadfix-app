import 'package:flutter/material.dart';

// 占位，Step 2 填充
class LoginPage extends StatelessWidget {
  const LoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('LoginPage placeholder')),
    );
  }
}
